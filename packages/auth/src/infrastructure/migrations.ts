/**
 * Migration definitions have been moved to individual SQL files.
 *
 * See `./migrations/sql/` for the per-migration `.sql` files.
 * The migration runner is at `./migrations/sql-migration.runner.ts`.
 *
 * This file is intentionally empty — the inline `PgMigrator.fromRecord`
 * approach has been replaced by file-based SQL migrations.
 */
